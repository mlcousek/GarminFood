node_modules/
.serena/

# Defensive: none of the tools currently write credential files into this
# repo, but if that ever changes, these patterns should catch it before a
# broad `git add` does.
*.env
.env.*
*token*.json
*credentials*.json
