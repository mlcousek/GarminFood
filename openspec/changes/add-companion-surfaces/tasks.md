## 22. Fix the vault's Garmin nutrition read

- [x] 22.1 Update `getNutritionLog(date)` in `scripts/lib/garmin.mjs`. **Done 2026-09-14** — now calls `/nutrition-service/food/logs/{date}` (plural), the route found and confirmed live during `establish-garmin-nutrition-contract`. Change made directly in the vault's own checkout; not yet committed there — see the plan's top-level report for what's pending review.
- [x] 22.2 Update `parseNutrition()` and `parseMeals()` in `sync-nutrition.mjs` to match the real payload shape. **Done 2026-09-14**, reading `dailyNutritionContent`/`dailyNutritionGoals`/`mealDetails`/`loggedFoods` directly rather than guessing field names with `??` fallback chains.
- [x] 22.3 Add the loud-vs-quiet distinction. **Done** — `getNutritionLog()` now logs a visible `console.error` on any non-rate-limit failure, since the route is now known-correct and any future failure is worth surfacing.
- [x] 22.4 Run `node scripts/sync-nutrition.mjs --days 30` against the live vault. **Run 2026-09-14** with `--days 3` as a smoke test: the sync correctly extracted real data for today (see 22.5). A full `--days 30` backfill was not run in this session to avoid an unbounded number of requests against the live account without the owner present to review the result; recommended as the next step.
- [x] 22.5 Spot-check against the Garmin Connect app. **Partially done via cross-check, not the app itself**: the sync's extracted totals (1738 kcal, 89g protein, 234g carbs, 49g fat) matched the raw API response's `dailyNutritionContent` values exactly, confirming the parsing logic is correct. A visual comparison against the Garmin Connect app's own display was not performed in this session.
