## 17. Controls (the primary lock-screen/Action-Button surface)

- [x] 17.1 Build a `ControlWidgetButton` per quick-pick food (from `add-food-log-core`'s local ranking), each with its own `AppIntent` carrying `authenticationPolicy = .alwaysAllowed`. **REVISED 2026-09-14 (design.md D1):** each intent uses `supportedModes = .foreground` (NOT the originally-planned `[.background]` — that ran `perform()` in the extension's own process, which has no Garmin credential and no access to this app's local data at all; see `add-garmin-auth-and-sync` task 6.4). Implemented: `Shared/QuickPickLoggingIntents.swift` (4 intents, `LogQuickPick1Intent`-`LogQuickPick4Intent`) + `GarminFoodWidget/Controls/QuickPickControls.swift` (the 4 `ControlWidget`s). Not built/CI-verified — no local Xcode/Swift toolchain (openspec/config.yaml's "no Mac" constraint); GitHub Actions CI is the real gate.
- [ ] 17.2 Verify a Control fires while the device is locked, and confirm the entry lands in the app process's local outbox (per `garmin-sync`) once the app-foreground escalation required by 17.1's revision has occurred. **Whether this still avoids a Face ID prompt is explicitly unconfirmed** (design.md D1's Risks section) — requires a real device test, not assumed.
- [ ] 17.3 **Neither half done.** `controlWidgetActionHint` was attempted, but CI's real compile rejected it (`ControlWidgetButton` has no such member) — removed rather than left as a guessed, broken API surface; the real modifier name/placement needs checking in Xcode before retrying. `controlWidgetStatus` feedback ("Logged 250 kcal") was never attempted for the same reason task 17.3 originally gave: no confirmed real-string example was found in available research.
- [x] 17.4 Eligibility for placing the primary quick-pick Control on the Action Button (iPhone 15 Pro+) requires no extra code — any Control is automatically offered for that slot by the system on eligible hardware. Documented as hardware-dependent, not a baseline requirement (design.md Open Question 1). The actual on-device placement is a one-time user action in Settings, outside this repo's scope.
- [x] 17.5 Build the barcode-scan Control (`OpenIntent`, target membership spanning app + extension) that opens the app directly into `BarcodeScanScreen`. Implemented: `Shared/OpenBarcodeScannerIntent.swift` + `GarminFoodWidget/Controls/ScanBarcodeControl.swift` + `AppNavigationBridge`/`FoodCatalogView` wiring to actually present the scanner once the app is foregrounded.

## 18. Lock Screen accessory (static)

**REVISED 2026-09-14 (design.md D2):** this widget shows NO data of any kind, not a read-only ring. `add-garmin-auth-and-sync` task 6.4 confirmed no App Group/Keychain Sharing exists on this account, so this extension process has no channel to Garmin's daily-summary route (or to anything else). The two tasks below are rewritten from their original wording accordingly.

- [x] 18.1 ~~Build the `accessoryCircular`/`accessoryRectangular` ring showing today's total, read fresh from Garmin's daily-summary route~~ → Build the `accessoryCircular`/`accessoryRectangular` widget as a static icon + `widgetURL` tap-to-open, with no data fetch of any kind. Implemented: `GarminFoodWidget/GarminFoodLockScreenWidget.swift` (`TimelineProvider` returns a single entry with `.never` refresh policy — there is nothing to refresh towards).
- [ ] 18.2 Confirm no interactive element is placed here (true by construction — the view has no buttons/toggles, only a `widgetURL`) and verify the family renders in `vibrant`/`accented` mode correctly with no `fullColor` assets (a single SF Symbol via `Label`/`Image`, `.widgetAccentable()` applied — should render correctly, but actual rendering in these modes needs Xcode Previews or a real device, neither available here).

## 19. Home Screen widget

**REVISED 2026-09-14 (design.md D2):** same reasoning as section 18 — this widget shows no data and has no quick-add buttons. Tasks 19.1-19.4 are rewritten/struck accordingly; 19.5 is largely moot for this widget specifically (it maintains no local store of its own to protect).

- [x] 19.1 ~~Build the `systemSmall` widget: `Gauge`-based ring plus 2-4 `Button(intent:)` quick-add tiles~~ → Build the `systemSmall` widget as a static icon + label + `widgetURL` tap-to-open, with no ring and no quick-add tiles (quick-add belongs to the Controls in section 17, which have a real path to the app's process; a widget's own `Button(intent:)` would hit the exact same App-Group problem). Implemented: `GarminFoodWidget/GarminFoodHomeWidget.swift`.
- [x] 19.2 ~~Implement the `TimelineProvider` reading the daily-summary route with a short cache~~ → Implement the `TimelineProvider` to fetch NOTHING and return `Timeline(..., policy: .never)` — there is no route this process can ever successfully call (no Garmin credential reachable from here). Implemented in GarminFoodHomeWidget.swift.
- [x] 19.3 ~~Show locally-pending (not-yet-reconciled) entries as a visually distinct provisional addition~~ — SUPERSEDED, not implemented: this would require reading the app's own Outbox file, which is exactly the App-Group-shared-storage problem this whole change is built around not having. There is no way to know pending-entry state from this process.
- [x] 19.4 ~~Verify a same-widget quick-add tap updates the ring instantly~~ — SUPERSEDED, not applicable: there is no quick-add tap and no ring on this widget (see 19.1).
- [ ] 19.5 Set the widget's local store's file protection to `NSFileProtectionCompleteUntilFirstUserAuthentication` — moot for this widget specifically, since it maintains no local store of its own (no data, nothing to persist). The file-protection requirement remains correctly satisfied where it actually matters: GarminKit's `Outbox`/`TokenProvider` and FoodLogCore's `UsageHistoryStore`/`ServingDefaultStore` already use `.completeUntilFirstUserAuthentication` (pre-existing, confirmed by reading their source). Verifying the widget still appears on the Mac via Continuity needs a real Mac + paired iPhone, neither available here.

## 20. Siri and Shortcuts

- [x] 20.1 Implement `AppShortcutsProvider` with 3 shortcuts: log the top quick-pick food, log a named food (parameterised), open the scanner. Confirmed each phrase includes `\(.applicationName)` by inspection (not by Xcode's own diagnostic, which requires a toolchain). Implemented: `GarminFood/Shortcuts/GarminFoodShortcuts.swift`, `LogTopQuickPickIntent.swift`, `LogNamedFoodIntent.swift`.
- [ ] 20.2 Donate each completed log via `IntentDonationManager` — implemented (best-effort, non-blocking) in both `LogTopQuickPickIntent`/`LogNamedFoodIntent`'s `perform()`. Deleting the donation when an entry is deleted is NOT implemented: this app has no existing user-facing "delete a logged entry" flow to hook the deletion into yet (only `Reconciliation.swift`'s own internal duplicate-cleanup calls `Outbox.delete(id:)`, not a UI action) — deferred until that flow exists, rather than inventing one out of this change's scope.
- [ ] 20.3 Verify via Xcode's App Shortcuts Preview that all phrases resolve correctly and the app stays well under the 10-shortcut cap. The count (3) is verifiable by inspection; the resolution check itself needs Xcode.

## 21. Cross-surface verification

**Milestone, 2026-09-15: the full app — all targets, all Controls, all Shortcuts, both widgets, and all three Swift packages (GarminKit, FoodLogCore, Gamification) — compiles cleanly and archives into a valid unsigned `.ipa` in CI.** Getting here took 4 fix cycles from the first attempt: a SwiftUI `Section` overload ambiguity, `MainActor` isolation on `DataScannerViewController`, an iOS-26-only API (`IntentModes`) used against an iOS-17 deployment target, a nonexistent `ControlWidgetButton` modifier, and a Siri phrase illegally embedding a free-form `String` parameter — each found only by CI, none by inspection, exactly as expected with no local Swift/Xcode toolchain available throughout this project (openspec/config.yaml's "no Mac" constraint).

All three tasks below still require a real, signed, installed build on a physical device (Controls, in particular, cannot be exercised in the iOS Simulator per Apple's own tooling limits) — none are verifiable in this environment. Left unchecked rather than assumed.

- [ ] 21.1 Log once from each surface (app, Control, widget quick-add, Siri) in one sitting and confirm all four entries reconcile correctly in Garmin with no duplicates. (Note: "widget quick-add" no longer exists per section 19's revision — this becomes three surfaces: app, Control, Siri.)
- [ ] 21.2 Verify the expired-token state (`add-garmin-auth-and-sync` D7) surfaces correctly on the widget and Control, not just in the app. (Note: the widgets are now data-free and have no auth-state display of their own — this reduces to verifying the Control's behavior when the token is expired, e.g. that its action fails loudly rather than silently.)
- [ ] 21.3 Time the fastest path (Action Button hold-to-log) end to end and confirm it is genuinely at or under two user actions.
