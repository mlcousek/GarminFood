## 1. Settle the entitlement question

- [x] 1.1 Confirm whether the Garmin account has an active Connect+ subscription. **Confirmed by owner, 2026-09-14: yes.**
- [~] 1.2 In the Garmin Connect iOS app, log three foods by hand: one searched generic food, one barcode-scanned packaged product, one custom food. **Searched-food logging done (2026-09-14).** **Barcode explicitly skipped by owner decision** — tried in the Garmin Connect app's own native scanner first and it did not recognize a Czech product barcode; see `docs/garmin-food-log-contract.md`'s new "Barcode scanning has poor Czech coverage" section. No further barcode probing planned for now. Custom-food logging not yet done.
- [x] 1.3 Re-run `node tools/probe-garmin-nutrition.mjs --date <that date> --dump`. Record every status code. **Done 2026-09-14**, then superseded by direct `tools/garmin-get.mjs` probing of the corrected routes once found (see task group 2) — the original probe's fixed list didn't have the right path to test in the first place.
- [x] 1.4 **Gate.** Record the verdict: does `consumedKilocalories` in `/usersummary-service/usersummary/daily` become non-null once food is logged? **Answered, but not the way this task expected: no, it stays null even with real logged food present.** The two systems are disconnected — `usersummary-service` is not fed by nutrition-service data at all. The real total lives at `dailyNutritionContent.calories` in `/nutrition-service/food/logs/{date}`. Project's premise is intact; the originally-assumed read path was simply wrong. See `docs/garmin-food-log-contract.md`.

## 2. Discover the food-log routes

- [x] 2.1 Obtain the Garmin Connect Android APK (version 5.20.0.20 or later). **Done 2026-09-14** — v5.29 (Sep 2026) downloaded via a public APK mirror (apkcombo.com) using the browser, since this could not be scripted directly. The XAPK bundle (401MB) was downloaded to the local Downloads folder; only its 148MB base APK (containing the code) was needed, and the full bundle should be deleted once findings are captured — it does not belong in the repo or in long-term storage.
- [x] 2.2 Decompile with `jadx`. **Substituted approach 2026-09-14**: no JVM was available in the working environment, so `classes*.dex` (14 multidex files) were extracted from the base APK with Python's stdlib `zipfile`, and scanned for printable-ASCII string runs via a simple regex rather than full jadx disassembly. This was sufficient — dex string pools store route paths and Kotlin `toString()` field-name fragments as plain ASCII, and grepping them found every `nutrition-service` route literal without needing a Java toolchain at all. Recorded in `docs/garmin-routes.json`.
- [x] 2.3 Extract the request DTOs for food-log create/update/delete: field names and enum constants. **Done, with a caveat**: field NAMES were found (`FoodLogRequestDTO(date=`, `mealType`, `foodId`, `servingId`, `DeleteFoodRequestDTO(logIds=`, etc.), but dex string pools are alphabetically sorted, which destroys the source order the `toString()` fragments originally appeared in — so field ORDER within the request body is inferred, not confirmed. Meal-type enum values confirmed live (not from decompilation): `BREAKFAST`, `LUNCH`, `SNACKS`. See `docs/garmin-food-log-contract.md`.
- [x] 2.4 Verify each discovered READ route live with `node tools/garmin-get.mjs <path>`. **Done for the two highest-value routes**: `food/logs/{date}` (200, real data) and `food/search/barCode` (400 without a param naming `barCode`; 404 for an unmatched code, confirming the route exists). The remaining read routes found in decompilation (`meals/{date}`, `food/recent/{asOfDate}`, `favorite/food`, `nutritionCurrentStatus`, `calorie/summary/daily`, metadata routes) are recorded but not yet individually verified — `observedStatus: null` in the registry marks exactly which.
- [x] 2.5 Mitmproxy/Proxyman fallback. **Not needed** — the decompilation approach in 2.2 succeeded on the first attempt and found the write route directly, so the iOS traffic-interception fallback was never exercised.

## 3. Build the registry and harness

- [x] 3.1 Create `docs/garmin-routes.json`. **Done 2026-09-14**, seeded with every route from both live probing and decompilation, each tagged with its actual observed status (including `null` for decompiled-but-unverified, and `"documented, not exercised"` for write routes per this project's own rule against automated writes).
- [x] 3.2 **DONE (audit 2026-09-16): probe-garmin-nutrition.mjs buildProbes already reads its route list from docs/garmin-routes.json.** Refactor `tools/probe-garmin-nutrition.mjs` to read its route list from `docs/garmin-routes.json`. Not yet done — the registry now exists and is far more complete than the probe's original hardcoded list, so this refactor is straightforward but still pending.
- [ ] 3.3 **CARRIED to add-app-shell-and-meal-dashboard 9.4.** Make the harness classify and report 400/401/402/403/404/429 distinctly. Partially satisfied by `tools/garmin-get.mjs`'s ad hoc output (which does print 400 bodies in full — that's how `searchExpression` and `barCode` were both found), but the *automated* harness (`probe-garmin-nutrition.mjs`) itself hasn't been updated to match yet.
- [x] 3.4 **Gate.** A write route was found and documented (`POST /nutrition-service/food/logs`, plus quick-add, bulk, delete, and custom-food/meal creation siblings). **Proceed** — the project's premise holds. No fallback needed.

## 4. Document the write contract

- [x] 4.1 Write `docs/garmin-food-log-contract.md`. **Done 2026-09-14.** Method, path, an inferred request body, the confirmed real read shape it must eventually match, and every open uncertainty, each with its own confidence level rather than a blanket "confirmed."
- [ ] 4.2 Record how an entry is identified for update/delete. **Answered for delete**: `DeleteFoodRequestDTO(logIds=...)` — a list of `logId` values (confirmed as a distinct field from `foodId` on read: a hex string unique per log entry). Update is unconfirmed — no `UpdateFoodLogRequestDTO`-shaped string was found; it may reuse the create route with the same `logId`, or not exist at all (delete-and-recreate may be the only option).
- [ ] 4.3 Record the idempotency story. **Still open.** No client-supplied idempotency key was found in any discovered DTO. `garmin-auth-and-sync`'s per-process outbox design already assumes the client must de-duplicate itself (via reconciliation against Garmin's own log) — this finding confirms that assumption was the right call, but the actual duplicate-request behavior (does posting the same entry twice create two rows, or does Garmin dedupe server-side?) remains unverified until a real write is attempted.
- [x] 4.4 Per-fact observation dates. **Done** — every fact in `docs/garmin-food-log-contract.md` is dated 2026-09-14, and the confidence level (confirmed-live vs. inferred-from-decompilation vs. genuinely unknown) is stated per fact rather than implied by the document's own date.

## 5. Close the loop on the vault

- [x] 5.1 Record the vault's broken route. **Done and fixed, not just recorded** — `scripts/lib/garmin.mjs`'s `getNutritionLog()` called the singular `/nutrition-service/food/log/date/{date}` (404), masked by a bare `catch { return null }`, since the day it was written. This has been corrected directly rather than waiting for a separate change.
- [x] 5.2 Note the corrected route for `add-companion-surfaces`. **Superseded by doing the fix immediately**: `getNutritionLog()` now calls the correct plural route and logs loudly on unexpected failures instead of swallowing them; `sync-nutrition.mjs`'s `parseNutrition()`/`parseMeals()` were rewritten to match the real response shape and verified against the live account (1738 kcal / 89g protein extracted matched the raw API response exactly). `add-companion-surfaces`'s tasks are now effectively already done.
