// SportBodySlotView.swift
//
// STUB (add-gamification-signals D12): the Progress-tab slot for the sport & body achievements.
// Ships empty; `add-sport-and-body-achievements` replaces this file's contents (and adds its
// own screens) without touching the host or any shared view. To reach its
// feature's own async API: `environment.gamificationEngine.featureHost?
// .feature(<FeatureType>.self)`; the generic hub-card data is
// `featureHost?.summaries[<featureId>]`.
//
// Depended on by: ProgressSlotHost.

import SwiftUI

@MainActor
struct SportBodySlotView: View {
    var body: some View {
        EmptyView()
    }
}
