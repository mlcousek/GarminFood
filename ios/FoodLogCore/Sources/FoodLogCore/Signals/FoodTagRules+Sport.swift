// FoodTagRules+Sport.swift
//
// STUB owned by `add-sport-and-body-achievements` (wave 2 of the gamification redesign). Ships
// empty from add-gamification-signals so `FoodTagRuleRegistry.all` already
// lists it; the owning change replaces this file's contents with its own
// rules (and declares its own tags in a new `FoodTag+Sport.swift`) without
// touching any shared file.

import Foundation

extension FoodTagRuleSet {
    public static let sport = FoodTagRuleSet(id: "sport", rules: [])
}
