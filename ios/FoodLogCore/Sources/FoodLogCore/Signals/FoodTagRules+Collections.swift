// FoodTagRules+Collections.swift
//
// STUB owned by `add-food-collections` (wave 2 of the gamification redesign). Ships
// empty from add-gamification-signals so `FoodTagRuleRegistry.all` already
// lists it; the owning change replaces this file's contents with its own
// rules (and declares its own tags in a new `FoodTag+Collections.swift`) without
// touching any shared file.

import Foundation

extension FoodTagRuleSet {
    public static let collections = FoodTagRuleSet(id: "collections", rules: [])
}
