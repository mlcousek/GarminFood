// FoodTagRules+Seasonal.swift
//
// STUB owned by `add-seasonal-events` (wave 2 of the gamification redesign). Ships
// empty from add-gamification-signals so `FoodTagRuleRegistry.all` already
// lists it; the owning change replaces this file's contents with its own
// rules (and declares its own tags in a new `FoodTag+Seasonal.swift`) without
// touching any shared file.

import Foundation

extension FoodTagRuleSet {
    public static let seasonal = FoodTagRuleSet(id: "seasonal", rules: [])
}
